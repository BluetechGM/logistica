SELECT 
    c.*,
    r.imagem_exemplo
FROM compras_so.catalogo_itens_checklist c
LEFT JOIN compras_so.checklist_semanal_referencias r
       ON r.item_codigo = c.codigo
WHERE c.ativo = TRUE
  AND c.tipo_checklist = 'SEMANAL'
ORDER BY c.label;